---
name: Manage labels

on:
  push:
    paths:
      - ".github/labels.yml"
      - ".github/workflows/manage-labels.yml"

jobs:
  manage-labels:
    name: Update Labels
    uses: CGBassPlayer/sicas-workflows/.github/workflows/manage-labels.yml@stable
    with:
      labels_file: labels.yml
      delete_missing: true
